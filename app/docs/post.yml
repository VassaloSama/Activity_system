tags:
  - Atividades
summary: Criar Atividade
requestBody:
  required: true
  content:
    application/json:
      schema:
        type: object
        properties:
          atividade_id:
            type: integer
            example: 1
          professor_id:
            type: integer
            example: 2
          enunciado:
            type: string
            example: "Atividade matemática"
responses:
  201:
    description: Atividade criada com sucesso
  400:
    description: Erro de validação